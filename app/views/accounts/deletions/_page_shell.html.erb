<div class="paywall-layout account-deletion-page" data-testid="<%= testid %>">
  <article class="paywall-layout__main panel panel--chrome">
    <h1 class="billing-page__title"><%= title %></h1>
    <%= yield %>
  </article>

  <%= render "accounts/deletions/aside" %>
</div>
