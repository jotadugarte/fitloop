<%= turbo_stream_from @project %>

<main
  class="page page--wide project-show"
  data-testid="project-show"
  data-controller="nesting-progress-sync"
  data-nesting-progress-sync-url-value="<%= nesting_sync_project_path(@project) %>"
  data-project-status="<%= @project.status %>"
>
  <header class="page-header page-header--compact">
    <h1><%= @project.title %></h1>
    <%= render "status_badge", project: @project %>
  </header>

  <section class="project-show__preview-zone">
    <%= render "source_dxf_preview", project: @project, source_preview: @source_dxf_preview %>
    <%= render "nesting_preview", project: @project, preview: @nesting_preview %>
  </section>

  <%= render "show_actions", project: @project %>

  <section class="project-show__progress">
    <%= render "nesting_progress", project: @project, eta_overrun: false, time_limit_notice: @time_limit_notice, orphans: @nesting_orphans %>
  </section>

  <section class="project-show__history">
    <%= render "nesting_run_history", nesting_runs: @nesting_runs %>
  </section>

  <section class="project-show__links panel panel--subtle">
    <h2 class="panel__title"><%= t("projects.show.manage_title") %></h2>
    <ul class="link-list">
      <li><%= link_to t("projects.show.layers"), project_layers_path(@project) %></li>
      <li><%= link_to t("projects.show.edit"), edit_project_path(@project) %></li>
      <li><%= link_to t("projects.show.back"), projects_path %></li>
    </ul>
  </section>
</main>
