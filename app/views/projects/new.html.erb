<% content_for :inline_flash, true %>
<main class="page page--wide" data-testid="project-setup">
  <h1><%= t("projects.new.setup_title") %></h1>
  <%= render "shared/flash" %>
  <%= render "setup_form", project: @project %>
</main>
